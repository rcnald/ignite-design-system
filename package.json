{
  "name": "ignite-ui",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "packageManager": "npm@10.8.1",
  "devDependencies": {
    "turbo": "^2.0.5"
  }
}
